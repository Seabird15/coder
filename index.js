let submit = prompt("Ingrese nombres de animales")
    while (submit != "Salir") {
        alert ("Ingresaste " + submit)
        submit = prompt ("ingrese otro nombre, escriba Salir para terminar")
    }

